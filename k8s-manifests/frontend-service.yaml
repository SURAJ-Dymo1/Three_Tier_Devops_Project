apiVersion: v1
kind: Service
metadata:
  name: three-tier-app-frontend
spec:
  type: NodePort
  selector:
    app: three-tier-app-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
