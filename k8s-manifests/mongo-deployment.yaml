apiVersion: apps/v1
kind: Deployment
metadata:
  name: three-tier-app-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: three-tier-app-mongo
  template:
    metadata:
      labels:
        app: three-tier-app-mongo
    spec:
      containers:
      - name: three-tier-app-mongo
        image: surajmane121045/three_tier_app_mongodb:ffb61c71e2c102331af734603a5039aa74268420
        ports:
        - containerPort: 27017
        volumeMounts:
        - name: mongo-storage
          mountPath: /data/db
      volumes:
      - name: mongo-storage
        emptyDir: {}


# kubectl describe pod -l app=mongo
